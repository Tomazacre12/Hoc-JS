var slave = {
    name: 'Nill Kiga',
    price: 0
}
var nig = slave.price > 0 ? `${slave.price} $` :'Free'
console.log(nig)

var slave2 = {
    name: 'Kill Niga',
    price: 10
}
var nig2 = slave2.price > 0 ? `${slave2.price} $` :'Free'
console.log(nig2)